FROM ubuntu:25.10

ENV DEBIAN_FRONTEND=noninteractive
RUN apt-get update && apt-get install -y \
  bash zsh git curl ca-certificates unzip \
  bats jq diffutils \
  && rm -rf /var/lib/apt/lists/*

WORKDIR /dotfiles
CMD ["bats","test/bats"]
